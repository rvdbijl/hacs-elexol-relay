# Elexol Relay v0.2.1

## Fixed

- Fixed a Home Assistant 500 error when opening the Add integration flow.
- Replaced the custom readback poll interval validator with a Home Assistant-native numeric selector so the config flow schema can be serialized for the UI.

## Notes

Restart Home Assistant after installing this update so the custom component Python module is reloaded.
