{
  "config": {
    "step": {
      "user": {
        "title": "Elexol Relay",
        "description": "Configure the Elexol Ether I/O 24 R relay board.",
        "data": {
          "host": "Host",
          "udp_port": "UDP port",
          "port_selection": "Installed relay ports",
          "poll_interval": "Readback poll interval seconds"
        }
      }
    },
    "abort": {
      "already_configured": "This Elexol relay board is already configured."
    },
    "error": {
      "host_required": "Enter the relay board host."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Elexol Relay options",
        "description": "Update the UDP port, installed relay ports, and readback polling.",
        "data": {
          "udp_port": "UDP port",
          "port_selection": "Installed relay ports",
          "poll_interval": "Readback poll interval seconds"
        }
      }
    }
  }
}
